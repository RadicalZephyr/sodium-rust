[package]
name = "adr-research"
version = "0.0.0"
edition = "2021"
# Nothing here is part of the published crate; it exists so the numbers quoted
# in an ADR can be re-derived instead of taken on trust.
publish = false
description = "Experiments backing the architecture decision records in docs/adr/"

[dependencies]
sodium-rust = { path = "../../.." }
